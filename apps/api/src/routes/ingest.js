/**
 * POST /api/ingest — NODE / GATE payload
 * TODO: cornerKg, vibration, audioRms doğrulama
 */
module.exports = function createIngestRouter(/* deps */) {
  const express = require("express");
  return express.Router();
};
