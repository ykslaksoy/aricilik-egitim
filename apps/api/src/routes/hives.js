/**
 * GET /api/hives, GET /api/hives/:id, POST /api/hives/:id/calibrate
 * TODO: server.js'den taşınacak
 */
module.exports = function createHivesRouter(/* deps */) {
  const express = require("express");
  const router = express.Router();
  // router.get("/", ...)
  return router;
};
