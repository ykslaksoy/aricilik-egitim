/**
 * arılık CRUD — gezginci modu
 * GET/POST /api/apiaries
 * POST /api/apiaries/:id/hives
 */
module.exports = function createApiariesRouter(/* deps */) {
  const express = require("express");
  return express.Router();
};
