# koloni mobil (plan)

iOS / Android — aynı REST API.

| ekran | api |
|-------|-----|
| liste | GET /api/hives |
| detay | GET /api/hives/:id |
| kalibrasyon | POST /api/hives/:id/calibrate |
| taşıma | POST /api/transport/* |
| push | FCM / APNs + alarm webhook |

framework: React Native veya Flutter (karar verilmedi).
