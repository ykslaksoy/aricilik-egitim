/**
 * SuperAriDevices — Demo | Canlı çalışma modu + cihaz durumları.
 * REVERT_PLACEHOLDER_TEST — will be replaced immediately with full pre-layout file.
 */
(function (global) {
  'use strict';
  global.__ARICILIK_REVERT_PROBE__ = true;
})(typeof window !== 'undefined' ? window : this);
