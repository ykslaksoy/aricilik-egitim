# Kamera ↔ IR karşılaştırma — opsiyonel

**Durum:** calisiyor (demo)

Sadece hem IR hem kovan kamerası varken.  
Kamera yoksa bu kalibrasyon atlanır; öğlen IR + tare yolu yeterli.

**Demo:** `hiveCamera.irComparison` — `deltaInPct`, `deltaOutPct`, `agrees` (±18% eşik)

**Bağlı:** `sensorler/kamera.md`, `sensorler/ir-sayac.md`
