# shared

| dosya | içerik |
|-------|--------|
| `constants.js` | **Sensör eşikleri**, oğul, skor, besleme, yavru, hava — tek kaynak |
| `ingest.schema.json` | kart → sunucu payload |

Doküman: `docs/ozellikler/sensorler/degerlendirme.md`  
API özeti: `GET /api/eval-config`
