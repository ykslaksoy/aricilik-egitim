# firmware — GATE (T-Beam + A7670E)

LoRa al → HTTPS POST → koloni API

| modül | dosya |
|-------|--------|
| LoRa RX | `lora_receive.cpp` |
| 4G forward | `cellular_forward.cpp` |

çalışınca NODE alır — WiFi testten sonra.
